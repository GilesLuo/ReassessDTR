from setuptools import setup, find_packages

setup(
    name='more',
    version='0.0.1',
    author='Zhiyao Luo',
    author_email='zhiyao.luo@eng.ox.ac.uk',
    description='Minimal Offline Reinforcement Learning Library',
    packages=find_packages(),
    install_requires=[
        # Add your dependencies here (if any)
    ],
)
